spring:
  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,config-schema

logging:
  level:
    root: info
    sun:
      rmi: error
    javax:
      management: error










